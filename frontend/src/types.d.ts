export interface CartItemType {
  id: number;
  category: string;
  description: string;
  imageUrl: string;
  price: number;
  name: string;
  stock: number;
  amount: number;
  stripePriceId: string;
}
